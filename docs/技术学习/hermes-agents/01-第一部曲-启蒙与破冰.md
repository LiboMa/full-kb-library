---
来源: 基于 NousResearch/Hermes-3 模型卡 + Hermes-Function-Calling 仓库 + arXiv:2408.11857
作者: 沙漠绿洲 (Ma Ronnie 知识库)
日期: 2026-05-06
标签: [hermes, agents, function-calling, nous-research, llm, agentic-ai]
Ground Truth: ✅ 基于 HuggingFace 模型卡 + GitHub 仓库 README + 技术报告
主题: 从零到一精通 Hermes Agents —— 第一部曲《启蒙与破冰》
系列: 三部曲 Part 1 / 3
前置: 懂一点 Python、用过 ChatGPT 即可
后续: 02-第二部曲-匠心与构建.md、03-第三部曲-问道与深潜.md
---

# 从零到一精通 Hermes Agents · 第一部曲：启蒙与破冰

> 这个系列的目标，是把 Hermes Agents 这条路线从"是什么"讲到"怎么改模型权重"。
> 第一部曲的任务很朴素：让你看懂它是什么、跑通第一个 Hello World、明白它和 OpenAI Function Calling 到底差在哪。
> 不需要你有 Agent 开发经验，会写 Python、装过 pip 包就能跟上。

---

## 1.1 什么是 Hermes？

先把名词拆开。

**Nous Research** 是一家 2023 年成立的开源 AI 研究组织，风格很硬核：不做闭源 API、不做套壳产品，专注在开源模型的后训练（post-training）上。他们最出圈的工作就是 Hermes 系列——一组把开源基座模型"调教"成优秀指令跟随者的微调模型。

**Hermes 系列** 的演进大致是这样：

    Hermes 1  (2023)  ——  基于 Llama 1 / Mistral，第一版通用指令微调
    Hermes 2  (2024)  ——  引入 ChatML，规范多角色对话格式
    Hermes 2 Pro      ——  首次加入强化 Function Calling 能力
    Hermes 3  (2024) ——  基于 Llama 3.1，8B/70B/405B 三档，Function Calling / JSON Mode / Agent 能力集大成

所以我们今天说的 "Hermes Agents"，准确说法是：**以 Hermes 3 模型为大脑、以其原生训练的 Function Calling 模式为交互协议的 Agent 范式**。它不是一个 pip 安装的框架，而是一种"模型原生的 Agent 工作方式"。

Nous 给 Hermes 3 定的核心哲学叫 **"Neutrally Aligned, User Controlled"**——中立对齐、用户掌控。翻译成人话就是：不搞过度的安全对齐，尽量让模型按用户的 system prompt 来行事，而不是内置一堆"我不能帮你做这个"的拒绝反射。这是和 GPT-4 / Claude 非常不一样的哲学选择，对 Agent 开发者而言友好得多——因为 Agent 经常需要模型严格按指令走，而不是动不动被安全对齐卡住。

和 Llama 3.1 的关系：Hermes 3 是 **Llama 3.1 的指令微调版本**，不是从头训练。这意味着它继承了 Llama 3.1 的基座能力（128K 上下文、多语言、代码）、架构（Transformer）、License（Llama 3.1 Community License），只是在 post-training 阶段被 Nous 用他们自己的数据集和训练目标重新塑造了"性格"和"工具使用能力"。

---

## 1.2 Hermes Agents 解决什么痛点？

如果你今天想做一个 Agent，主流选项大概三种：调 OpenAI API、用 LangChain / AutoGen 套一个框架、或者直接拿开源模型自己搞。每条路都有坑。

**痛点 1：闭源 API 贵且不可控。** GPT-4 Function Calling 做 Agent 确实丝滑，但每次 tool call 往返都在烧钱，而且你的 prompt、tool schema、用户数据全部过人家服务器。企业场景里的合规问题、金融/医疗数据的外泄风险、离线环境的刚需——API 路线根本迈不过去。

**痛点 2：通用开源模型不擅长结构化输出。** 你拿 Llama 3.1 原版去做 tool call，会发现它时不时输出不合法的 JSON、忘记闭合标签、函数名拼错。不是模型笨，是它没被专门训练过这种输出模式。Agent 最怕的就是"十次里有一次 parse 失败"，这种概率会在循环调用里被放大成灾难。

**痛点 3：框架引入过重抽象。** LangChain 最被诟病的一点就是"什么都包一层"——AgentExecutor、Tool、Chain、Memory、Callback 层层叠叠，调一个 bug 要穿三层继承。框架本身没错，但它把本该透明的"模型输出什么 → 我怎么执行"这件事藏进了黑盒。

Hermes 的答案很干脆：**把 Tool Use 训进模型权重里，去掉中间商**。你不需要 LangChain 的 AgentExecutor，因为模型自己就懂 `<tool_call>` 该长什么样；你不需要担心 JSON 格式崩坏，因为它在训练时就被反复教过这件事。你要做的只剩两件：给它工具定义、执行它输出的调用。

---

## 1.3 核心概念解析

要读懂后面的代码，先建立五个概念。它们都是 Hermes Agents 日常交互的基本单元。

**ChatML 格式**。这是 OpenAI 最早提出、Hermes 沿用的多角色对话格式。核心是用特殊 token `<|im_start|>` 和 `<|im_end|>` 包裹消息，每条消息带一个角色标签：

    <|im_start|>system
    你是一个助手...
    <|im_end|>
    <|im_start|>user
    今天天气怎么样？
    <|im_end|>
    <|im_start|>assistant
    ...

Hermes 3 在训练时就看这种结构长大，所以 tokenizer 里的 `<|im_start|>` / `<|im_end|>` 是真实的 special token，不是普通字符。

**Tool Definition**。在 system prompt 里用 `<tools>` XML 标签声明可用工具，内容是标准 JSON Schema：

    <tools>
    [{"type": "function", "function": {"name": "get_weather", "description": "...", "parameters": {...}}}]
    </tools>

这个格式和 OpenAI Chat Completions API 的 `tools` 参数几乎一模一样，所以迁移成本极低。

**Tool Call**。模型决定要调用函数时，输出会是这样：

    <tool_call>
    {"name": "get_weather", "arguments": {"city": "Beijing"}}
    </tool_call>

你的代码要做的就是正则或 XML 解析抓出这段 JSON。

**Tool Response**。你执行完函数后，把结果以 `tool` 角色回注进对话历史：

    <|im_start|>tool
    <tool_response>
    {"temperature": 22, "condition": "sunny"}
    </tool_response>
    <|im_end|>

**GOAP (Goal Oriented Action Planning)**。这是 Hermes 3 一个更高级的能力：当任务复杂时，它会在 `<scratch_pad>` 里做类似 Goal / Actions / Observation / Reflection 的显式推理，相当于模型自带"思考过程"。这个我们放第二部曲细讲。

---

## 1.4 环境准备（5 分钟快速搭建）

选一条适合你的路：

**方案 A：vLLM 本地部署**（推荐生产/多用户场景）

    pip install vllm
    vllm serve NousResearch/Hermes-3-Llama-3.1-8B \
        --host 0.0.0.0 --port 8000

起来之后是一个 OpenAI 兼容端点，任何支持 OpenAI SDK 的客户端都能直连。需要一张 24G 显存的卡（4090 / A10 / A100 都行）跑 FP16，显存不够就加 `--quantization awq` 用 4bit。

**方案 B：HuggingFace Transformers + 4bit 量化**（笔记本能跑）

    pip install transformers bitsandbytes accelerate

然后在代码里用 `BitsAndBytesConfig(load_in_4bit=True)` 加载 `NousResearch/Hermes-3-Llama-3.1-8B`。8B 模型 4bit 量化后大约占 6GB 显存，一张 8G 的 RTX 3070 勉强能跑。

**方案 C：Ollama 一键部署**（最简单，本篇 Hello World 用这个）

    # macOS/Linux
    curl -fsSL https://ollama.com/install.sh | sh
    ollama pull hermes3
    ollama serve   # 启动服务，默认监听 http://localhost:11434

Ollama 内部会拉 GGUF 量化版本，CPU 也能跑（慢一些），有 Apple Silicon 的 Mac 可以跑得飞快。它同样暴露 OpenAI 兼容端点，路径在 `/v1/chat/completions`。

装完验证一句话：

    curl http://localhost:11434/api/generate -d '{"model":"hermes3","prompt":"hi","stream":false}'

有正常回复就说明环境就绪。

---

## 1.5 实践环节：5 分钟 Hello World Agent

目标：做一个能查天气的最简 Agent。不用任何 Agent 框架，纯 Python + requests，30 行左右代码，走完完整循环。

前提：Ollama 已经 `ollama pull hermes3` 并启动。

```python
import json, re, requests

OLLAMA = "http://localhost:11434/v1/chat/completions"

# 1. 真实工具函数（演示用，实际可接天气 API）
def get_weather(city: str) -> dict:
    fake_db = {"Beijing": {"temp": 22, "cond": "sunny"},
               "Shanghai": {"temp": 26, "cond": "cloudy"}}
    return fake_db.get(city, {"temp": 20, "cond": "unknown"})

# 2. 工具 schema（JSON Schema 标准）
tools_schema = [{"type": "function", "function": {
    "name": "get_weather",
    "description": "Get current weather for a city",
    "parameters": {"type": "object",
                   "properties": {"city": {"type": "string"}},
                   "required": ["city"]}}}]

# 3. ChatML system prompt，把 tools 嵌进去
system_prompt = f"""You are a helpful assistant with tool access.
<tools>
{json.dumps(tools_schema)}
</tools>
When you need a tool, output:
<tool_call>{{"name": "...", "arguments": {{...}}}}</tool_call>"""

messages = [{"role": "system", "content": system_prompt},
            {"role": "user",   "content": "北京现在天气怎么样？"}]

def chat(msgs):
    r = requests.post(OLLAMA, json={"model": "hermes3", "messages": msgs, "stream": False})
    return r.json()["choices"][0]["message"]["content"]

# 4. 第一轮：模型输出 tool_call
reply = chat(messages)
print("Assistant (round 1):", reply)

# 5. 解析 <tool_call>
m = re.search(r"<tool_call>\s*(\{.*?\})\s*</tool_call>", reply, re.S)
if m:
    call = json.loads(m.group(1))
    result = get_weather(**call["arguments"])        # 执行函数
    # 6. 回注 tool_response，再聊一轮
    messages += [{"role": "assistant", "content": reply},
                 {"role": "tool", "content": f"<tool_response>{json.dumps(result)}</tool_response>"}]
    final = chat(messages)
    print("Assistant (final):", final)
```

跑起来后你会看到第一轮模型输出类似 `<tool_call>{"name":"get_weather","arguments":{"city":"Beijing"}}</tool_call>`，第二轮拿到 tool 结果后用自然语言回你："北京现在 22 度，晴天……"。

这就是一个 Agent 的最小闭环：**用户问 → 模型决定调工具 → 代码执行 → 结果回注 → 模型综合回答**。所谓 Agent 框架，做的复杂事情本质就是把这个循环变成多轮、多工具、带错误处理的版本——但内核就是这 30 行。

---

## 1.6 与 OpenAI Function Calling 的对比

写完 Hello World 你可能已经察觉：这玩意儿跟 OpenAI Function Calling 长得真像。没错，格式几乎兼容。但背后的差异决定了它们的使用场景。

| 维度 | Hermes Agents (本地) | OpenAI Function Calling |
|------|----------------------|-------------------------|
| 格式 | ChatML + `<tool_call>` XML | `tools` 参数 + `tool_calls` JSON 字段 |
| 部署 | 本地 / 私有云 | OpenAI 云端 API |
| 成本 | GPU 一次性硬件投入 | 按 token 付费，长期累加 |
| 速率限制 | 无（取决于你的 GPU） | RPM / TPM 限流 |
| 数据隐私 | 完全不出本地 | Prompt 过 OpenAI 服务器 |
| 可定制性 | 可 LoRA 微调、改 system prompt 无上限 | 只能调参数，无法改权重 |
| 离线可用 | ✅ | ❌ |
| 生态兼容 | vLLM 暴露 OpenAI 兼容端点，无缝切换 | 原生 |
| 延迟 | 取决于本地硬件，可控 | 受网络 + 服务端队列影响 |
| 模型迭代 | 你决定何时升级 | OpenAI 随时 deprecate 旧模型 |

**关键一点**：vLLM / Ollama 起的服务都实现了 OpenAI 兼容端点。也就是说，一个已经用 `openai` SDK 写好的应用，把 `base_url` 改成 `http://localhost:8000/v1`、`api_key` 填 "dummy"，就能无缝切到 Hermes 上。迁移成本几乎为零，这是 Hermes 路线最大的工程友好之处。

成本上算一笔账：GPT-4o 做 Agent，平均一次 tool call 往返约 2000 token 输入 + 500 输出，$0.0125 左右；跑一万次就是 125 美元。而一张 4090 本地跑 Hermes-3-8B，24 小时不停机就摊薄到电费水平。对需要高频调用的 Agent 场景（爬虫助手、数据分析、自动化运维），这个差距会很快拉开。

当然代价也有：GPT-4 级别的推理质量，8B 的 Hermes 3 确实打不过；70B 和 405B 版本能摸到 GPT-4 的门，但对硬件的要求也陡升。**选型原则**：简单结构化任务（查询、分类、格式转换、固定工作流）用 Hermes 8B 足够；复杂多步推理、开放域问答，老实上 70B 或 GPT-4。

---

## 小结

第一部曲到这里，你应该已经：

- 搞清楚 Hermes 是谁家的（Nous Research）、是什么（Llama 3.1 指令微调）、什么哲学（中立对齐、用户掌控）
- 看懂 ChatML / `<tools>` / `<tool_call>` / `<tool_response>` 四件套
- 跑通了一个 30 行的 Hello World Agent
- 心里有一张"什么时候选 Hermes，什么时候还是乖乖用 OpenAI"的决策表

Hello World 只是起点。真实世界的 Agent 要解决的是：多个工具怎么编排？递归调用怎么控制深度？模型怎么把推理过程"想明白"？怎么让 Agent 有记忆、能和别的 Agent 协作？怎么把结构化输出打磨到工业级稳定？

这些就是下一篇要回答的。

---

**下一篇**：[02-第二部曲-匠心与构建.md](./02-第二部曲-匠心与构建.md)

**参考资料**：
- Hermes 3 模型卡：https://huggingface.co/NousResearch/Hermes-3-Llama-3.1-8B
- 技术报告：https://arxiv.org/abs/2408.11857
- Function Calling 仓库：https://github.com/NousResearch/Hermes-Function-Calling
