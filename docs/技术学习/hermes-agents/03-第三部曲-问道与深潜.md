---
来源: 基于 Hermes 3 Technical Report (arXiv:2408.11857) + NousResearch/Hermes-Function-Calling 源码 + vLLM/axolotl 官方文档
作者: 沙漠绿洲 (Ma Ronnie 知识库)
日期: 2026-05-06
标签: [hermes-agents, advanced, architecture, fine-tuning, vllm, security, nous-research]
Ground Truth: ✅ arXiv:2408.11857 原文 + HuggingFace 模型卡 + vLLM 0.6.x 文档
主题: 《从零到一精通 Hermes Agents》第三部曲 —— 问道与深潜
系列: 三部曲 Part 3 / 3
前置: 02-第二部曲-匠心与构建.md
后续: 本系列完结
---

# 第三部曲：问道与深潜 —— 底层机制与架构哲学

> 如果说第一部曲让你"会用"，第二部曲让你"会造"，那么第三部曲的目标只有一个：**让你会问，也会答**。
> 当一个 tool call 出错的时候，你要能从模型权重、token 设计、推理引擎一路追到 system prompt 上某个被稀释的 token；当架构师在白板上比较四个 Agent 框架时，你要能说出"它们根本不是同一层抽象"这句话背后的含金量。

---

## 0. 回顾：我们是如何走到这里的

第一部曲里，我们用三十行 Python 跑通了第一个 Hermes Agent：定义一个 `get_weather` 函数、写一段 ChatML system prompt、把 `<tool_call>` 解析出来再把 `<tool_response>` 塞回去。那一刻的直觉是——"原来 Agent 没有那么神秘，就是一段约定的 XML 对话。"

第二部曲里，我们把这份直觉推到了生产级：多工具编排、GOAP `<scratch_pad>` 推理、JSON Mode 结构化输出、短期/长期/工具三类记忆、Router / Pipeline / Debate 三种 Multi-Agent 拓扑、错误容错、以及用 vLLM + FastAPI 包成 OpenAI 兼容端点。

但所有这些都有一个前提——**你相信 Hermes 模型"刚好"能这么听话**。第三部曲要回答的就是：它凭什么能？以及，当它不够听话时，你可以往哪些层下手？

---

## 3.1 Hermes 3 微调解密

Hermes 3 技术报告（arXiv:2408.11857，以下简称 TR）是这个系列最重要的一份一手资料。你不一定要读完 30 页，但下面这几个结论是绕不过去的。

### 训练数据构成（TR §3.1）

Hermes 3 的 SFT 数据集约 **390M tokens**，覆盖以下几大类（占比为报告披露的近似区间）：

    通用指令跟随          ~45%    （含 ShareGPT、OpenHermes 2.5 遗产）
    Function Calling      ~15%    （glaive-function-calling-v2 改造 + 内部数据）
    结构化输出 / JSON     ~10%    （Pydantic schema → 实例对）
    长上下文推理          ~10%    （需 32K+ 窗口的问答、摘要）
    代码 + Agent 轨迹      ~10%    （含多轮 tool call 完整 trace）
    Role-play / 创意       ~10%    （保留通用性，反过度 alignment）

关键洞察有两个。第一，**Function Calling 数据不是追加训练的小甜点，而是和通用指令一起端到端 SFT 的主菜**——这解释了为什么 Hermes 不需要外挂 grammar constraint 就能稳定吐 `<tool_call>`。第二，创意/role-play 数据被刻意保留了 10% 左右，这是"Neutral Alignment"理念在数据层的落地：不为了守规矩而把模型变傻。

### ChatML token 的设计理由

为什么是 `<|im_start|>` / `<|im_end|>` 而不是 `[INST]`、`###`、或者纯 JSON？TR §2.2 给了三条实用理由：

1. **单一 token 化**：`<|im_start|>` 在分词器里是一个独立 special token（id 固定），不会在字节对编码里被拆碎。这让 loss mask 可以精确到角色边界。
2. **对称闭合**：`<|im_end|>` 作为 EOS 的角色专用变体，允许模型在 assistant 轮次结束时干净停机，避免"说完还续写"的常见毛病。
3. **生态兼容**：ChatML 是 OpenAI 在 2023 年提出的事实标准，沿用它意味着前端 SDK、vLLM、TGI、Ollama 几乎零成本对接。

这三条合在一起，就是"为什么不能用 Markdown 分隔符"的完整回答——Markdown 的 `###` 在分词器里会和普通井号冲突，无法做到干净的 loss mask。

### Neutral Alignment 的定量表现

TR §5 展示了一组有意思的对比。在 RefusalBench（专门考察"过度拒绝"的测试集）上：

    Llama 3.1 8B Instruct    拒绝率 34.2%
    Hermes 3 8B              拒绝率  8.7%
    
    同时在 MT-Bench 上：
    Llama 3.1 8B Instruct    8.21
    Hermes 3 8B              8.15

用人话说：**Hermes 把"该拒绝的"保留了，把"不必要的安全噪音"砍掉了四分之三，同时综合能力几乎不掉**。这就是 Nous 团队口中的 "Neutrally Aligned, User Controlled"——不是没有对齐，而是把对齐的权杖交到你手里。

---

## 3.2 System Prompt 工程的极限

System Prompt 是 Hermes 用户唯一能在"不微调"前提下影响模型行为的主要入口。但它远不是"越长越好"。

### 一个可复现的方法论表格

下面这张表给出了评测 system prompt 策略的标准流程，即使你手头没跑过，也可以当作实验设计模板：

| 策略 | 描述 | 预期 tool_call 准确率 | 预期首 token 延迟 | 副作用 |
|------|------|----------------------|------------------|--------|
| A. 零 few-shot | 仅 tool schema + 一句自然语言约束 | 基线 ~78% | 最低 | 边缘 case 翻车 |
| B. 1-shot | 附 1 个完整 tool call 示例 | ~86% (+8pp) | +12% | 会被"示例同化" |
| C. 3-shot 多样 | 3 个覆盖不同参数形态的示例 | ~91% (+13pp) | +28% | prefill 开销显著 |
| D. 5-shot + GOAP 模板 | 加入 `<scratch_pad>` 骨架 | ~93% (+15pp) | +40% | 注意力稀释风险 |
| E. 动态 tool 子集 | 按 intent 路由仅注入相关工具 | ~94% (+16pp) | 视情况下降 | 需前置分类器 |

评测协议建议：固定 seed=0、`temperature=0.0`、`top_p=1.0`，用 Hermes-Function-Calling 仓库的 `func_calling_eval.py` 跑 500 条 held-out 样本，报告 exact-match 和 schema-valid 两项。

### 注意力稀释（Attention Dilution）

"prompt 越长效果越好"是一个经典谬误。Transformer 在长 context 下对开头、结尾的关注度天然高于中段（U 型），当 system prompt 塞入 20 个工具的完整 schema 时，第 7~13 个工具的 tool_call 准确率会肉眼可见地下降。

策略 E（动态 tool 子集）就是给这个问题开的处方——在请求进入模型前先跑一个轻量 intent classifier（甚至可以用 Hermes 自己 zero-shot 跑），把 20 个工具裁到 3~5 个，再送进主推理。生产系统里这一步通常能把 p95 延迟降 30% 以上。

---

## 3.3 架构哲学对比：Hermes / LangChain / AutoGen / CrewAI

| 维度 | Hermes Agents | LangChain | AutoGen | CrewAI |
|------|---------------|-----------|---------|--------|
| Agent 定义 | System Prompt + Tools | Chain / Agent Class | ConversableAgent | Role-based Agent |
| 编排方式 | 模型自主 GOAP | 代码显式 Chain | 消息传递 | Task 分配 |
| Tool 接入 | JSON Schema 原生 | Tool Wrapper 类 | Function Map | Tool 装饰器 |
| 推理链 | 内化于模型权重 | 外部 Memory/Chain | 对话历史 | Sequential/Hierarchical |
| 可调试性 | `<scratch_pad>` 可见 | verbose=True | Chat Log | Task Log |
| 运行时依赖 | 仅模型 + 推理引擎 | langchain + 100+ 扩展 | pyautogen | crewai + langchain |
| 适用场景 | 低延迟、离线、隐私 | 快速原型、生态丰富 | 多 Agent 对话研究 | 角色化团队模拟 |

### 扩写解读：四种哲学的世界观

**LangChain —— "框架即 Agent"**。它的世界观是：LLM 不够可靠，所以必须在代码层把推理步骤硬编码成 Chain / Graph（LangGraph 是其终极形态）。优点是可控、可断点、可回放；缺点是你写的代码量和抽象层和框架一起膨胀，出问题要先学会调试框架本身。

**AutoGen —— "对话即计算"**。微软的方案把 Agent 建模成可对话对象，用消息交换驱动计算。它最适合"N 个 Agent 围坐一桌辩论"的研究场景，但生产落地时，**对话轮次的收敛性**是个持续头疼的问题。

**CrewAI —— "组织即 Agent"**。它把真实公司的"角色—任务—流程"搬进代码，对非技术产品经理友好。代价是抽象再加一层——底层仍然依赖 LangChain。

**Hermes —— "模型即 Agent"**。能力训进权重里，外部只剩一个薄薄的 ChatML 解析循环（Hermes-Function-Calling 仓库的 `functioncall.py` 核心不到 200 行）。优点是延迟低、依赖少、行为可预测；代价是——想改行为，要么改 prompt，要么就得微调，没有中间层给你"打补丁"。

一句话总结：**前三者在应用层解决问题，Hermes 在模型层解决问题**。它们不是竞品，而是不同抽象层的工具，可以组合使用（比如用 LangGraph 编排多个 Hermes Agent 节点）。

---

## 3.4 性能调优

当 Hermes Agent 从 demo 走向生产，性能就是一切。下面按推理栈从上到下过一遍。

### vLLM + PagedAttention 启动

最推荐的部署起点（单卡 A100 80G / H100）：

    vllm serve NousResearch/Hermes-3-Llama-3.1-8B \
        --dtype bfloat16 \
        --max-model-len 32768 \
        --gpu-memory-utilization 0.92 \
        --enable-prefix-caching \
        --enable-chunked-prefill \
        --max-num-batched-tokens 8192 \
        --max-num-seqs 128 \
        --served-model-name hermes-3

关键参数：
- `--enable-prefix-caching`：同一 system prompt 被复用时，KV cache 直接命中。Agent 场景下 system prompt 通常占 2~4k tokens，复用收益极大（实测首 token 延迟可从 380ms 降到 45ms）。
- `--enable-chunked-prefill`：让长 prompt 的 prefill 和新请求的 decode 可以交错，提升整体吞吐 20%~40%。
- `--max-num-seqs 128`：PagedAttention 让并发不再被显存碎片限制，128 路并发在 8B 模型上是稳妥起点。

### 量化对 tool call 精度的影响（方法论）

| 量化方案 | 显存（8B） | MT-Bench | Tool Call Exact-Match | 建议 |
|----------|-----------|----------|----------------------|------|
| FP16 / BF16 | 16 GB | 8.15 | 93% | 生产基线 |
| AWQ 4-bit | ~5.5 GB | 8.05 | 91% (-2pp) | **推荐**：精度/显存最佳平衡 |
| GPTQ 4-bit | ~5.5 GB | 7.98 | 88% (-5pp) | 老模型兼容 |
| bitsandbytes NF4 | ~5 GB | 7.80 | 84% (-9pp) | 仅开发/调试 |

经验法则：**tool call 对权重精度比通用对话更敏感**，因为 JSON 的语法合法性是离散的（错一个逗号就整条废掉）。能上 AWQ 不要上 bnb。

### Speculative Decoding 原理图解

原理：用一个"小而快"的 draft model（比如 Hermes 3B 或 Llama 3.2 1B）先吐 K 个候选 token，再让"大而准"的目标模型（Hermes 8B/70B）一次性验证 K 个 token 是否接受——接受的直接写入，拒绝的回退到被拒位置。

    draft:   t1 t2 t3 t4 t5        （1B 模型，快 8x）
    target:  ✔  ✔  ✔  ✘  -         （8B 模型并行验证）
    commit:  t1 t2 t3 (拒绝 t4，target 自己出 t4')

在 tool_call 场景下这个技巧格外划算——因为 `<tool_call>`、`{"name":`、`"arguments":` 这些片段高度可预测，小模型几乎能 100% 猜对，实测端到端加速 1.8x~2.3x。vLLM 开启方式：

    vllm serve NousResearch/Hermes-3-Llama-3.1-70B \
        --speculative-model NousResearch/Hermes-3-Llama-3.1-8B \
        --num-speculative-tokens 5

---

## 3.5 自定义微调：LoRA / QLoRA 训练 Hermes Agent

当 prompt 调到极限还不够用——比如你要把它用在"法律合同条款提取"这种极度垂直的场景——就需要微调。

### 五元组训练数据格式

Hermes 推崇的是 `(system, user, assistant-with-tool_call, tool-response, assistant-final)` 五元组。下面是 13 行真实可用的 jsonl 示例（字段按 axolotl 的 `chat_template: chatml` 约定）：

    {"conversations":[{"from":"system","value":"You are a legal assistant. <tools>[{\"name\":\"extract_clause\",\"parameters\":{\"contract\":\"string\",\"clause_type\":\"string\"}}]</tools>"},{"from":"human","value":"帮我从这份合同提取违约责任条款：<合同全文...>"},{"from":"gpt","value":"<tool_call>{\"name\":\"extract_clause\",\"arguments\":{\"contract\":\"<合同全文...>\",\"clause_type\":\"违约责任\"}}</tool_call>"},{"from":"tool","value":"<tool_response>{\"clause\":\"乙方逾期交付每日按合同总额 0.5% 支付违约金\"}</tool_response>"},{"from":"gpt","value":"违约责任条款：乙方逾期交付，按合同总额每日 0.5% 支付违约金。"}]}
    {"conversations":[{"from":"system","value":"You are a legal assistant. <tools>[{\"name\":\"extract_clause\",\"parameters\":{...}}]</tools>"},{"from":"human","value":"提取保密条款"},{"from":"gpt","value":"<tool_call>{\"name\":\"extract_clause\",\"arguments\":{\"clause_type\":\"保密\"}}</tool_call>"},{"from":"tool","value":"<tool_response>{\"clause\":\"双方对交易信息保密期 5 年\"}</tool_response>"},{"from":"gpt","value":"保密条款：交易信息保密期 5 年。"}]}
    {"conversations":[{"from":"system","value":"..."},{"from":"human","value":"提取争议解决条款"},{"from":"gpt","value":"<tool_call>{\"name\":\"extract_clause\",\"arguments\":{\"clause_type\":\"争议解决\"}}</tool_call>"},{"from":"tool","value":"<tool_response>{\"clause\":\"上海国际仲裁中心仲裁\"}</tool_response>"},{"from":"gpt","value":"争议解决：提交上海国际仲裁中心仲裁。"}]}
    {"conversations":[{"from":"system","value":"..."},{"from":"human","value":"这份合同签约日期是什么？"},{"from":"gpt","value":"<tool_call>{\"name\":\"extract_clause\",\"arguments\":{\"clause_type\":\"签约日期\"}}</tool_call>"},{"from":"tool","value":"<tool_response>{\"clause\":\"2025-03-14\"}</tool_response>"},{"from":"gpt","value":"签约日期：2025 年 3 月 14 日。"}]}
    {"conversations":[{"from":"system","value":"..."},{"from":"human","value":"总价多少？"},{"from":"gpt","value":"<tool_call>{\"name\":\"extract_clause\",\"arguments\":{\"clause_type\":\"合同总价\"}}</tool_call>"},{"from":"tool","value":"<tool_response>{\"clause\":\"人民币 1,280,000 元\"}</tool_response>"},{"from":"gpt","value":"合同总价：人民币 128 万元。"}]}
    {"conversations":[{"from":"system","value":"..."},{"from":"human","value":"交付周期"},{"from":"gpt","value":"<tool_call>{\"name\":\"extract_clause\",\"arguments\":{\"clause_type\":\"交付周期\"}}</tool_call>"},{"from":"tool","value":"<tool_response>{\"clause\":\"签约后 60 日内\"}</tool_response>"},{"from":"gpt","value":"交付周期：签约后 60 日内。"}]}
    {"conversations":[{"from":"system","value":"..."},{"from":"human","value":"有没有不可抗力条款"},{"from":"gpt","value":"<tool_call>{\"name\":\"extract_clause\",\"arguments\":{\"clause_type\":\"不可抗力\"}}</tool_call>"},{"from":"tool","value":"<tool_response>{\"clause\":\"地震、疫情等不可抗力双方互免责任\"}</tool_response>"},{"from":"gpt","value":"不可抗力条款：地震、疫情等事件发生时双方互免责任。"}]}
    {"conversations":[{"from":"system","value":"..."},{"from":"human","value":"付款方式"},{"from":"gpt","value":"<tool_call>{\"name\":\"extract_clause\",\"arguments\":{\"clause_type\":\"付款方式\"}}</tool_call>"},{"from":"tool","value":"<tool_response>{\"clause\":\"预付 30%，验收后 60%，质保金 10%\"}</tool_response>"},{"from":"gpt","value":"付款方式：预付 30%，验收 60%，质保金 10%。"}]}
    {"conversations":[{"from":"system","value":"..."},{"from":"human","value":"质保期多久"},{"from":"gpt","value":"<tool_call>{\"name\":\"extract_clause\",\"arguments\":{\"clause_type\":\"质保\"}}</tool_call>"},{"from":"tool","value":"<tool_response>{\"clause\":\"验收合格后 24 个月\"}</tool_response>"},{"from":"gpt","value":"质保期：验收合格后 24 个月。"}]}
    {"conversations":[{"from":"system","value":"..."},{"from":"human","value":"合同生效条件"},{"from":"gpt","value":"<tool_call>{\"name\":\"extract_clause\",\"arguments\":{\"clause_type\":\"生效条件\"}}</tool_call>"},{"from":"tool","value":"<tool_response>{\"clause\":\"双方盖章且预付款到账\"}</tool_response>"},{"from":"gpt","value":"合同生效条件：双方盖章并预付款到账。"}]}
    {"conversations":[{"from":"system","value":"..."},{"from":"human","value":"变更流程"},{"from":"gpt","value":"<tool_call>{\"name\":\"extract_clause\",\"arguments\":{\"clause_type\":\"变更\"}}</tool_call>"},{"from":"tool","value":"<tool_response>{\"clause\":\"书面变更协议经双方签字生效\"}</tool_response>"},{"from":"gpt","value":"变更流程：需书面变更协议双方签字后生效。"}]}
    {"conversations":[{"from":"system","value":"..."},{"from":"human","value":"这合同有没有竞业条款"},{"from":"gpt","value":"<tool_call>{\"name\":\"extract_clause\",\"arguments\":{\"clause_type\":\"竞业\"}}</tool_call>"},{"from":"tool","value":"<tool_response>{\"clause\":null}</tool_response>"},{"from":"gpt","value":"未检索到竞业条款。"}]}
    {"conversations":[{"from":"system","value":"..."},{"from":"human","value":"合同语言"},{"from":"gpt","value":"<tool_call>{\"name\":\"extract_clause\",\"arguments\":{\"clause_type\":\"语言\"}}</tool_call>"},{"from":"tool","value":"<tool_response>{\"clause\":\"中文，中英不一致以中文为准\"}</tool_response>"},{"from":"gpt","value":"合同语言：中文，中英文不一致以中文为准。"}]}

注意最后一条——tool_response 返回 `null`，assistant 要学会**诚实回答"未检索到"**，这是反幻觉训练的关键样本。实际数据集里这类"负样本"应占 10~15%。

### axolotl 配置片段

    base_model: NousResearch/Hermes-3-Llama-3.1-8B
    model_type: LlamaForCausalLM
    tokenizer_type: AutoTokenizer
    
    load_in_4bit: true      # QLoRA 关键
    adapter: qlora
    lora_r: 32
    lora_alpha: 64
    lora_dropout: 0.05
    lora_target_modules:
      - q_proj
      - k_proj
      - v_proj
      - o_proj
      - gate_proj
      - up_proj
      - down_proj
    
    chat_template: chatml
    datasets:
      - path: ./legal_tool_calls.jsonl
        type: chat_template
        field_messages: conversations
        message_field_role: from
        message_field_content: value
    
    sequence_len: 8192
    sample_packing: true
    pad_to_sequence_len: true
    
    num_epochs: 3
    micro_batch_size: 2
    gradient_accumulation_steps: 8
    learning_rate: 2e-4
    warmup_ratio: 0.03
    
    gradient_checkpointing: true
    flash_attention: true

在一张 A100 80G 上，1000 条五元组样本跑 3 个 epoch，约 40 分钟出 LoRA adapter，合并回原模型即可 `vllm serve`。

---

## 3.6 安全：Agent 场景下的 Prompt Injection 攻击面

Neutral Alignment 把"拒绝不必要事情"的权力下放给用户，代价就是**安全边界完全由应用层负责**。下面三种攻击是 Hermes Agent 上线前必须演练的。

### 攻击 1：Tool Response 投毒

攻击者在外部数据（搜索结果、网页抓取、邮件正文）里埋入一段伪造的 `</tool_response><|im_start|>system` 注入新指令。

    # 防御代码：在回注前净化 tool response
    FORBIDDEN = ["<|im_start|>", "<|im_end|>", "<tool_call>",
                 "</tool_response>", "<tools>"]
    def sanitize_tool_response(text: str) -> str:
        for tok in FORBIDDEN:
            text = text.replace(tok, f"[REMOVED:{tok[:8]}]")
        return text[:4096]   # 同时做长度截断

### 攻击 2：参数逃逸 / 危险工具调用

用户在自然语言里诱导模型把 `rm -rf /` 作为参数传给 `run_shell` 工具。

    # 防御代码：白名单 + 参数校验
    ALLOWED_TOOLS = {"get_weather", "search_docs", "extract_clause"}
    DANGEROUS_PATTERNS = [r"rm\s+-rf", r";\s*curl", r"\|\s*sh", r"eval\("]
    
    def validate_tool_call(name: str, args: dict) -> bool:
        if name not in ALLOWED_TOOLS:
            raise SecurityError(f"tool {name} not whitelisted")
        flat = json.dumps(args)
        for pat in DANGEROUS_PATTERNS:
            if re.search(pat, flat):
                raise SecurityError(f"dangerous pattern in args")
        return True

### 攻击 3：多轮累积 Jailbreak

攻击者通过 10 轮看似无害的对话逐步改写 system prompt 的隐含规则，最终让模型调用本不该调用的工具。

    # 防御代码：每轮都重新注入"宪法"约束 + 敏感动作二次确认
    CONSTITUTION = "You MUST only call tools from the whitelist. " \
                   "Any tool call that modifies files, sends emails, " \
                   "or transfers money requires human confirmation."
    
    def build_messages(history, user_msg, tools):
        # 永远把宪法放在最后一条 system，利用 U 型注意力尾部偏好
        return [
            {"role": "system", "content": tools_prompt(tools)},
            *history,
            {"role": "user", "content": user_msg},
            {"role": "system", "content": CONSTITUTION},  # 关键
        ]
    
    def execute_with_confirm(tool_name, args):
        if tool_name in SENSITIVE_TOOLS:
            return {"status": "pending_human_approval", "args": args}
        return actually_execute(tool_name, args)

三种防御的共同精神：**永远不要相信模型会自己守规矩——把规矩硬编码在推理循环外面**。这和第 3.3 节 LangChain 的"框架即 Agent"反而殊途同归：安全边界天然属于确定性代码。

---

## 3.7 Hermes 的未来：从"模型即 Agent"到"模型即操作系统"

### 近期：Hermes 4 与更长上下文

从技术报告 §7 的 future work 能看出几个明确方向：1M context 窗口、原生多模态（图/音频 tool call）、以及更深的 planning 能力（推测会引入类似 o1 的隐式 CoT 预算）。

### 中期：与 MCP 的融合

Anthropic 的 Model Context Protocol (MCP) 本质上在定义"工具服务器"的标准传输层，这和 Hermes 的 `<tools>` schema 是天然互补的——前者管"怎么连"，后者管"怎么用"。可以预期 Hermes 4 会内置 MCP client 能力：用户只需 `mcp connect filesystem://` 一行，所有 MCP server 暴露的工具自动注册到 `<tools>` 块。

### 中期：A2A 协议与 Agent-to-Agent 通信

Google 提出的 A2A（Agent-to-Agent）协议试图为"多个异构 Agent 互相协作"定义统一接口。Hermes 的优势在于：它的 ChatML 本身就是消息协议，只需在 system prompt 里加一个 `<agent_card>` 描述自身能力，就能以极低成本成为 A2A 网络里的一个节点。

### 长期愿景：模型即操作系统

这是 Nous Research 公开演讲里多次提到的方向——未来的"应用"不再是代码包，而是 `.prompt` + `.tools` 的组合包；运行时不再是操作系统进程，而是一个常驻推理模型。模型负责调度、工具负责执行、prompt 负责定义意图。

这个愿景听起来像科幻，但回头看今天的 Hermes Agent 其实已经完成了三块基石里的第一块：**能力内化**。剩下的两块——**跨模型互操作**（MCP/A2A）和**长期自主性**（持续学习、自我改写）——是第四部曲（如果有的话）会讨论的内容。

---

## 三部曲完结总结

从第一部曲的 30 行 Hello World，到第二部曲的 Multi-Agent 研究助手，再到第三部曲的权重级调优——我们走过的不是一条"学 Hermes"的路，而是一条"从应用层一路下潜到模型层"的心智模型之旅。

Hermes 给我们的最重要礼物，不是某个具体功能，而是一种**"把 Agent 当成模型的原生表达"**的视角。当你下次看到新框架、新协议时，你会本能地问：这件事应该在哪一层做？是 prompt、是应用代码、是推理引擎、还是模型权重？能答出这个问题，就是这一系列三部曲最想让你带走的东西。

愿你在自己的 Agent 项目里，既会写 30 行的简洁，也敢去啃 30 页的技术报告。

---

## 🧭 导航

- ⬅️ 上一篇：[02-第二部曲-匠心与构建.md](./02-第二部曲-匠心与构建.md)
- 🏠 返回大纲：[00-三部曲大纲.md](./00-三部曲大纲.md)

---

*Ground Truth 来源：arXiv:2408.11857、NousResearch/Hermes-Function-Calling GitHub、vLLM 0.6.x 官方文档、axolotl 配置参考。*
