# 《从零到一精通 Hermes Agents》系列博文三部曲

> **目标技术**: Hermes Agents (NousResearch/Hermes 3 + Function Calling)  
> **作者**: Ma Ronnie 知识库  
> **创建日期**: 2026-05-06  
> **技术基线**: Hermes 3 (基于 Llama 3.1), arXiv:2408.11857  
> **核心仓库**: [NousResearch/Hermes-Function-Calling](https://github.com/NousResearch/Hermes-Function-Calling)  
> **标签**: `#hermes` `#agents` `#function-calling` `#nous-research` `#llm` `#agentic-ai`  

---

## 技术定位

**Hermes Agents 不是一个框架，而是一种"模型原生 Agent 模式"**：

| 维度 | Hermes Agents | LangChain/AutoGen |
|------|---------------|-------------------|
| 本质 | 微调模型 + 推理模式 | 应用层框架 |
| 依赖 | 仅需模型权重 + 推理引擎 | 需要安装框架 + 抽象层 |
| Tool Use | 模型原生训练支持 | 框架层拦截/路由 |
| 推理链 | GOAP scratch_pad 内化 | 外部 Chain/Graph 编排 |
| 哲学 | 用户对齐、中立、可控 | 生态优先、插件丰富 |

---

## 第一部曲：启蒙与破冰（概念与 Hello World）

### 目标受众
小白/初学者 — 有基础 Python 能力，了解 LLM 但未接触过 Agent 构建

### 核心议题
Hermes Agents 是什么？它和 ChatGPT Function Calling 有什么关系？为什么选择开源本地 Agent？

### 大纲条目

#### 1.1 什么是 Hermes？
- Nous Research 简介 — 开源 AI 研究组织
- Hermes 系列演进: Hermes 1 → Hermes 2 Pro → **Hermes 3**
- 核心哲学: **"Neutrally Aligned, User Controlled"（中立对齐，用户掌控）**
- 与 Llama 3.1 的关系 — Hermes 3 是 Llama 3.1 的指令微调版本

#### 1.2 Hermes Agents 解决什么痛点？
- **痛点 1**: 闭源 API Agent（GPT-4）成本高、数据外泄风险
- **痛点 2**: 通用开源模型不擅长结构化 Tool Use（输出格式不稳定）
- **痛点 3**: 框架（LangChain）引入过重抽象，调试黑盒
- **Hermes 的答案**: 模型层面原生训练 Function Calling，无需框架胶水层

#### 1.3 核心概念解析
- **ChatML 格式** — `<|im_start|>system/user/assistant/tool<|im_end|>` 多角色对话结构
- **Tool Definition** — `<tools>` XML 标签内的 JSON Schema 函数签名
- **Tool Call** — `<tool_call>` 模型输出的结构化函数调用
- **Tool Response** — `<tool_response>` 执行结果回注
- **GOAP (Goal Oriented Action Planning)** — `<scratch_pad>` 推理框架（Goal → Actions → Observation → Reflection）

#### 1.4 环境准备（5 分钟快速搭建）
- 方案 A: **vLLM 本地部署** — `vllm serve NousResearch/Hermes-3-Llama-3.1-8B`
- 方案 B: **HuggingFace Transformers + 4bit 量化** — 笔记本 GPU 可跑
- 方案 C: **Ollama 一键部署** — `ollama pull hermes3`
- 依赖: `pip install transformers bitsandbytes flash-attn`

#### 1.5 实践环节：5 分钟 Hello World Agent
- **目标**: 构建一个能查询天气的最简 Agent
- Step 1: 定义一个 `get_weather(city)` 函数
- Step 2: 编写 ChatML system prompt（含 tool schema）
- Step 3: 发送用户消息 → 模型输出 `<tool_call>`
- Step 4: 解析 tool call → 执行函数 → 回注 `<tool_response>`
- Step 5: 模型生成自然语言最终回复
- **完整代码**: 30 行 Python，零框架依赖

#### 1.6 与 OpenAI Function Calling 的对比
- 格式兼容性: ChatML ≈ OpenAI Chat API 格式
- 差异: 本地运行 / 无速率限制 / 完全可控 / 可微调
- 成本: GPU 一次性投入 vs API 按 token 付费

---

## 第二部曲：匠心与构建（核心工作流与实战应用）

### 目标受众
初中级开发者 — 已跑通 Hello World，想构建生产级 Agent 应用

### 核心议题
Tool Use 高级模式、记忆机制、Multi-Agent 协作、结构化输出

### 大纲条目

#### 2.1 多工具编排（Multi-Tool Orchestration）
- 单次请求触发多个 tool call（并行调用）
- 递归调用: `max_depth` 参数控制调用深度
- Tool 依赖链: Tool A 的输出作为 Tool B 的输入
- **实战**: 构建一个"股票研究助手"
  - `get_stock_fundamentals(symbol)` → 基本面
  - `get_financial_statements(symbol)` → 财报
  - `get_stock_price(symbol)` → 实时价格
  - 模型自动规划调用顺序并综合分析

#### 2.2 GOAP 推理框架深入
- `<scratch_pad>` 结构详解:
  ```
  Goal: <重述用户任务>
  Actions: <规划需要调用的函数>
  Observation: <工具返回结果摘要>
  Reflection: <评估任务完成状态>
  ```
- GOAP 如何让模型"想清楚再行动"
- 与 ReAct (Reason + Act) 的对比
- 何时启用 GOAP vs 简单 tool call

#### 2.3 结构化输出（JSON Mode）
- Pydantic Model → JSON Schema → 模型强制输出合规 JSON
- `jsonmode.py` 工作流详解
- **实战**: 构建数据提取 Agent — 从非结构化文本中提取结构化实体

#### 2.4 记忆机制设计
- **短期记忆**: ChatML 多轮对话上下文
- **长期记忆**: 向量数据库 + RAG 检索注入 system prompt
- **工具记忆**: 将历史 tool call + response 作为 few-shot 注入
- 上下文窗口管理: Hermes 3 支持 128K context (Llama 3.1)
- **实战**: 为 Agent 添加"记住上次对话"能力

#### 2.5 Multi-Agent 协作模式
- 模式 1: **Router Agent** — 一个主 Agent 分发任务给专家 Agent
- 模式 2: **Pipeline Agent** — Agent A 输出 → Agent B 输入（串行）
- 模式 3: **Debate Agent** — 多个 Agent 对同一问题辩论收敛
- 每种模式的 ChatML prompt 模板设计
- **实战**: 构建"自动数据研究助手"
  - Agent 1 (Researcher): 搜索信息、提取数据
  - Agent 2 (Analyst): 分析数据、生成洞察
  - Agent 3 (Writer): 整合输出、生成报告
  - Orchestrator: 协调三者执行顺序

#### 2.6 错误处理与容错
- Tool call 解析失败的 fallback 策略
- 模型产生非法 JSON 时的重试机制
- 超时和异常的优雅降级
- 日志与可观测性: 记录完整 tool call trace

#### 2.7 部署为 API 服务
- vLLM + FastAPI 包装 Agent 为 REST 端点
- OpenAI 兼容端点: 直接对接现有工具链
- 流式响应: SSE 输出中间推理过程

---

## 第三部曲：问道与深潜（底层机制与架构哲学）

### 目标受众
高级工程师/架构师 — 想理解 Hermes 为什么能做到这些，以及如何定制底层行为

### 核心议题
模型微调逻辑、System Prompt 工程极限、架构哲学对比、性能调优

### 大纲条目

#### 3.1 Hermes 3 微调解密
- 训练数据策略: 如何构造 Function Calling 训练数据
- ChatML token 设计: 为什么用 `<|im_start|>/<|im_end|>` 而非其他分隔符
- Tool Use 训练目标: 让模型学会在"何时调用"和"调用什么"之间做决策
- Neutrally Aligned 理念: 不过度 alignment，保留模型的通用性和可控性
- 技术报告 (arXiv:2408.11857) 关键发现解读

#### 3.2 System Prompt 工程的极限
- System Prompt 的最大可控边界在哪里？
- 少样本注入 (Few-shot) 对 Tool Use 精度的影响
- "System Prompt 越长效果越好"的谬误 — 注意力稀释问题
- 动态 System Prompt: 根据任务类型切换不同的 tool set
- **实验**: 不同 system prompt 策略下的 tool call 准确率对比

#### 3.3 架构哲学对比

| 维度 | Hermes Agents | LangChain | AutoGen | CrewAI |
|------|---------------|-----------|---------|--------|
| **Agent 定义** | System Prompt + Tools | Chain/Agent Class | ConversableAgent | Role-based Agent |
| **编排方式** | 模型自主 GOAP | 代码显式 Chain | 消息传递 | Task 分配 |
| **Tool 接入** | JSON Schema 原生 | Tool Wrapper 类 | Function Map | Tool 装饰器 |
| **推理链** | 内化于模型 | 外部 Memory/Chain | 对话历史 | Sequential/Hierarchical |
| **可调试性** | `<scratch_pad>` 可见 | Verbose=True | Chat Log | Task Log |
| **适用场景** | 低延迟、离线、隐私 | 快速原型、生态丰富 | 多 Agent 对话 | 团队模拟 |

**核心哲学差异**:
- LangChain: "框架即 Agent" — 编排在代码层
- AutoGen: "对话即计算" — Agent 通过消息协议协作
- **Hermes: "模型即 Agent"** — 能力内化于模型权重，最小化外部依赖

#### 3.4 性能调优
- **推理加速**: vLLM PagedAttention + Continuous Batching
- **量化策略**: 4bit (GPTQ/AWQ) vs 8bit vs FP16 的 tool call 精度影响
- **高并发**: Token 级 batching 下多用户 Agent 请求的隔离
- **Prefill 优化**: 长 system prompt 的 KV Cache 复用
- **Speculative Decoding**: 用小模型加速大模型 tool call 生成

#### 3.5 自定义底层行为
- 微调自己的 Tool Use 模型: LoRA / QLoRA 方案
- 训练数据格式: 如何构造 (system, user, assistant-tool_call, tool-response, assistant) 五元组
- 定制 GOAP 推理模板: 修改 `<scratch_pad>` 内部结构
- 添加新的 XML 特殊标签: 扩展模型的结构化输出能力
- **实战**: 为特定领域（如金融/医疗）微调一个专属 Hermes Agent

#### 3.6 安全与对齐
- Hermes "Neutral Alignment" 的安全取舍
- Tool Use 的安全边界: 如何防止模型调用危险工具
- Prompt Injection 在 Agent 场景下的新攻击面
- 防御策略: Tool 白名单、参数校验、沙箱执行

#### 3.7 Hermes 的未来路线图
- Hermes 4 预期方向（更大 context、更强 planning）
- 与 MCP (Model Context Protocol) 的融合潜力
- Agent-to-Agent 通信标准化
- 从"模型即 Agent"到"模型即操作系统"的演进

---

## 📚 参考资料

| 资源 | 链接 |
|------|------|
| Hermes 3 模型 (8B) | https://huggingface.co/NousResearch/Hermes-3-Llama-3.1-8B |
| Hermes 3 Technical Report | https://arxiv.org/abs/2408.11857 |
| Function Calling 代码仓库 | https://github.com/NousResearch/Hermes-Function-Calling |
| GGUF 量化版本 | https://huggingface.co/NousResearch/Hermes-3-Llama-3.1-8B-GGUF |
| ChatML 格式规范 | https://github.com/openai/openai-python/blob/main/chatml.md |
| Nous Research 官网 | https://nousresearch.com |

---

## 🗓️ 写作计划

| 部曲 | 预计字数 | 预计耗时 | 前置条件 |
|------|----------|----------|----------|
| 第一部曲 | 3000-4000 字 | 1-2 天 | 已具备全部素材 |
| 第二部曲 | 5000-7000 字 | 2-3 天 | 需跑通代码示例 |
| 第三部曲 | 4000-6000 字 | 2-3 天 | 需阅读技术报告全文 |

---

*Ground Truth 来源: HuggingFace 模型卡、GitHub 仓库 README、arXiv 技术报告*
